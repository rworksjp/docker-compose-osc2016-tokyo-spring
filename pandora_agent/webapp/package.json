{
  "name": "sample-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf public && webpack && cp source/index.html public/",
    "start": "node app.js"
  },
  "dependencies": {
    "body-parser": "~1.13.2",
    "express": "~4.13.1",
    "lodash": "^4.3.0"
  }
}
